# frozen_string_literal: true

RSpec.describe Spec::O::Matic do
  it "has a version number" do
    expect(Spec::O::Matic::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
